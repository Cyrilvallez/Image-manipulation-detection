---
name : Test_norms
date generated: Thursday 24/03/2022
GPU(s): dev machine (1 rtx)
CPUs: dev machine (unknown at this point)
dataset used: BSDS500, with attacks on disk
thresholds: np.linspace(0, 0.8, 20)
algorithms:  [
    hashing.NeuralAlgorithm('SimCLR v2 ResNet50 2x', raw_features=True, batch_size=150,
                            device='cuda', distance='cosine'),
    hashing.NeuralAlgorithm('SimCLR v2 ResNet50 2x', raw_features=True, batch_size=150,
                            device='cuda', distance='L2'),
    hashing.NeuralAlgorithm('SimCLR v2 ResNet50 2x', raw_features=True, batch_size=150,
                            device='cuda', distance='L1'),
    hashing.NeuralAlgorithm('SimCLR v2 ResNet50 2x', raw_features=True, batch_size=150,
                            device='cuda', distance='Jensen-Shannon')
    ]
general batch size: 32

---
purpose: |
Test the newly implemented L1 and L2 distances and compare them to cosine and Jensen-Shannon.